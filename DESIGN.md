# YamaLog Design System — Trail Dark

写真のEXIFだけで山行を復元する個人用アーカイブのための、暗い地に写真を置くデザイン。
**UIを書く前に必ずこのファイルを読むこと。** ここに無い色・サイズ・角丸を使わない。

前身は `getdesign apple`（明るいプロダクトカタログ）だったが、次の理由で作り直した。

- 画面のほとんどが山の写真で、明るい地だと写真が浮いて見え、白い雪や霧の写真は境界が消える
- セクションごとに地の色を変える構成（白→パーチメント→黒）は、道具として使う画面では落ち着かない
- 上部の横スクロールするナビは、iPhone を片手で持つと指が届かない

---

## 原則

1. **写真が主役、UIは沈む。** 地は暗く彩度を持たない。色を持つのは写真と、ただ1つのアクセントだけ。
2. **面の重なりで階層を作る。** 影は使わない。背景 → カード → 前面（シート）と、明度を1段ずつ上げる。
3. **境界は極細。** 区切りは `--line`（白8%）の1px。太い枠線・二重線は作らない。
4. **数字は等幅。** 距離・標高・時刻は `font-variant-numeric: tabular-nums`。桁が揺れると読み比べられない。
5. **親指の届く場所に操作を置く。** 主要な移動は画面下のタブバー。破壊的な操作は上に置かない。
6. **状態は文章で言う。** 空・失敗・処理中は、アイコンではなく短い日本語で説明する。

---

## Colors

### 地と面

| トークン | 値 | 用途 |
|---|---|---|
| `--bg` | `#0B0C0F` | 画面全体の地。ここより暗い面は作らない |
| `--surface` | `#15171C` | カード、入力欄、タブバー |
| `--surface-raised` | `#1D2027` | モーダル／シート、選択中のチップ |
| `--surface-sunken` | `#101216` | 写真グリッドの下地、コードや表の帯 |
| `--line` | `rgba(255,255,255,0.09)` | 1pxの境界。すべての区切りはこれ |
| `--line-strong` | `rgba(255,255,255,0.16)` | 入力欄のフォーカス、選択中のカード |

### 文字

| トークン | 値 | 用途 |
|---|---|---|
| `--text` | `#ECEEF1` | 本文・見出し。純白は使わない（写真の白と競合するため） |
| `--text-muted` | `#9AA1AB` | 補足、単位、キャプション |
| `--text-faint` | `#6B717A` | 無効状態、法的表記 |
| `--text-on-accent` | `#07131F` | アクセント面に載る文字（濃紺寄りの黒） |

### アクセント（1色）

| トークン | 値 | 用途 |
|---|---|---|
| `--accent` | `#5AA9FF` | すべての操作系。リンク、主ボタン、選択、フォーカスリング |
| `--accent-press` | `#3D8FE8` | 押下時 |
| `--accent-soft` | `rgba(90,169,255,0.14)` | アクセントの面（選択中のチップ、進捗バーの地） |

**アクセントは1色だけ。** 2色目の「ブランドカラー」を足さない。

### データ色（装飾に使わない）

意味を持つ図表・地図のピンにだけ使う。ボタンや見出しには使わない。

| トークン | 値 | 用途 |
|---|---|---|
| `--data-summit` | `#5BD6A0` | 登頂済みの山、成功件数 |
| `--data-pending` | `#7A828C` | 未登の山、未確定の値 |
| `--data-warn` | `#E9B872` | 補間値・推定値・EXIF欠損の注記 |
| `--data-danger` | `#E8796F` | 失敗件数、削除の確認文 |

---

## Typography

**Font:** `system-ui, -apple-system, 'Hiragino Sans', 'Noto Sans JP', sans-serif`
iPhone では SF Pro + ヒラギノに解決される。Webフォントは読み込まない（山中の回線を前提にする）。

| トークン | サイズ / 太さ / 行間 / 字間 | 用途 |
|---|---|---|
| `--t-display` | 30 / 700 / 1.2 / -0.02em | 画面タイトル（1画面に1つ） |
| `--t-title` | 22 / 700 / 1.3 / -0.015em | セクション見出し、山行名 |
| `--t-heading` | 17 / 600 / 1.35 / -0.01em | カードの見出し |
| `--t-body` | 16 / 400 / 1.6 / 0 | 本文 |
| `--t-caption` | 14 / 400 / 1.5 / 0 | 補足、リストの副情報 |
| `--t-label` | 13 / 600 / 1.2 / 0.01em | ラベル、タブ、チップ |
| `--t-fine` | 12 / 400 / 1.5 / 0 | 注記、出典 |
| `--t-stat` | 26 / 700 / 1.1 / -0.02em | 統計値（tabular-nums 必須） |

- 見出しは **700**、カード見出しは 600、本文は 400。500は使わない。
- 本文は **16px / 行間1.6**。日本語は字面が大きいので、17px だと1行が短くなりすぎる。
- 大きい文字ほど字間を詰める（-0.01 〜 -0.02em）。12〜14px では詰めない。

---

## Layout

**Spacing:** 4の倍数。`--s1` 4 · `--s2` 8 · `--s3` 12 · `--s4` 16 · `--s5` 20 · `--s6` 24 · `--s8` 32 · `--s10` 40 · `--s12` 48

- 画面の左右余白は **16px**（`--s4`）。iPhone の画面幅では、これ以上広げると写真が小さくなる。
- カード内の余白は 16px。カード同士の間隔は 12px。
- セクションの上下は 32px。見出しと本文の間は 8px。
- コンテンツの最大幅は **720px**（読み物）／ **1120px**（グリッド）。それ以上は中央寄せで余白に逃がす。

**Radius:** `--r-sm` 8（チップ・小ボタン）· `--r-md` 12（入力欄・写真セル）· `--r-lg` 16（カード）· `--r-xl` 22（シート）· `--r-pill` 9999（タブ・主ボタン）

**影:** 使わない。唯一の例外は前面のシート（`0 -8px 32px rgba(0,0,0,0.5)`）で、これは地との分離のためであって装飾ではない。

---

## Components

### タブバー（`.tab-bar`）

画面下に固定。5項目（ホーム / 山行 / 未分類 / 取り込み / 設定）。

- 背景 `--surface` + `backdrop-filter: blur(20px)`、上辺に `--line` の1px
- 高さ 56px + `env(safe-area-inset-bottom)`
- 各項目は アイコン20px + ラベル11px の縦積み、タップ領域は最低 44×56
- 選択中はアイコンと文字が `--accent`、非選択は `--text-faint`
- 未分類バッジは `--accent` の丸、数字は11px。**画面遷移のたびに数え直す**（古い数字を残さない）

### ページヘッダ（`.page-head`）

各画面の先頭。タイトル（`--t-display`）＋ 右に主アクション1つまで。固定しない（スクロールで流す）。
副情報（件数など）はタイトルの下に `--t-caption` で置く。

### カード（`.card`）

`--surface` / 1px `--line` / `--r-lg` / padding 16px。影なし。
押せるカードは押下時に `transform: scale(0.985)`。

### ボタン

| 種類 | 見た目 | 用途 |
|---|---|---|
| `.btn` | `--accent` 地 + `--text-on-accent`、`--r-pill`、高さ44、太さ600 | 画面の主アクション（1画面に1つ） |
| `.btn-quiet` | `--surface-raised` 地 + `--text`、`--r-pill`、高さ44 | 副アクション |
| `.btn-sm` | 高さ36、13px | カード内・行内のアクション |
| `.link` | `--accent` の文字だけ | 移動、取り消し |
| `.btn-danger` | 文字色 `--data-danger`、地なし | 削除。確認シートの中でのみ実行を確定する |

押下時は `scale(0.96)`。フォーカスは `outline: 2px solid --accent` + `outline-offset: 2px`。

### 入力（`input` / `select` / `textarea`）

`--surface` 地、1px `--line`、`--r-md`、高さ44、文字16px（**iOS で拡大されないよう16px未満にしない**）。
フォーカスで枠を `--accent`。ラベルは上に `--t-label` / `--text-muted`。

### 写真グリッド（`.photo-grid`）

- 2列（〜480px）／3列（〜734px）／4列（〜1024px）／5列（それ以上）。`dense` 指定で1段増やす
- セルは 1:1、`--r-md`、`object-fit: cover`、地は `--surface-sunken`
- 選択中は 2px `--accent` の内側リング（`box-shadow: inset 0 0 0 2px`）＋ 左上にチェック
- バッジ（時刻・撮影者・座標なし）はセル下部、`rgba(0,0,0,0.55)` + blur の上に11px

### シート（`.sheet`）

モーダルは画面下から出るシート。`--surface-raised`、上角 `--r-xl`、最大高さ 88vh、`--s6` の内側余白。
背景は `rgba(0,0,0,0.6)`。閉じるのは背景タップ、または右上の「閉じる」。

### 統計（`.stat-grid`）

2列（スマホ）／4列（デスクトップ）。各セルはラベル（`--t-label` / `--text-muted`）→ 値（`--t-stat`）。
推定値には値の直後に `推定` のチップ（`--data-warn`）を置く。

### 注記（`.notice`）

`--surface` 地 + 左に3pxの `--data-warn`、`--r-md`、14px。エラーは `--data-danger`。

### 地図（`.map`）

国土地理院タイルは明るいので、周囲のUI（レイヤ切替・凡例）は `rgba(0,0,0,0.6)` + blur の上に置く。
地図自体には色フィルタをかけない（地形の判読性を落とさないため）。

---

## Do / Don't

**Do**
- 面の明度差と1pxの線だけで階層を作る
- 操作は下に、情報は上に置く
- 数字は等幅、単位は `--text-muted` の小さい文字で添える
- 空状態には「次に何をすればよいか」を1文で書き、そこへのリンクを置く

**Don't**
- 影・グラデーション・ぼかしを装飾として使わない（blur はタブバーとシートの機能的な用途のみ）
- アクセント以外の色をボタンやリンクに使わない
- 1画面に主ボタン（`.btn`）を2つ以上置かない
- 16px未満のフォントを入力欄に使わない（iOS が自動でズームする）
- セクションごとに地の色を変えない

---

## Responsive

| 幅 | 変化 |
|---|---|
| ≤ 480px | 写真2列、統計2列、タブバー5項目、左右余白16px |
| 481–734px | 写真3列 |
| 735–1024px | 写真4列、統計4列、タブバーは下のまま（タッチ端末を想定） |
| ≥ 1025px | 写真5列、コンテンツ最大1120px、タブバーは画面下中央に浮かせて最大幅640px |

タップ領域は最低 44×44。行内リンクだけは例外で、上下に12pxの余白を持たせて実質44pxにする。

---

## 実装メモ

- `color-scheme: dark` を宣言し、フォーム部品を暗くする
- 高さは `100dvh`（iOS のアドレスバーで切れないように）
- タブバーとシートは `env(safe-area-inset-bottom)` を加算する
- 画像は `loading="lazy"`、`decoding="async"`
